# Config sensibil / local — nu există în producție, se setează din Coolify (env vars)
config/config.local.php
config/config.local.php.example

# Doar pentru dev local (php -S), nu pentru Apache/producție
router.php
scripts/

# Repo / VCS
.git
.gitignore

# Documente de dev, nu fac parte din aplicație
wireframes/
reports/
*.md
